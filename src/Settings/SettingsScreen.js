import React from 'react';
import {Text, View} from 'react-native';

const SettingsScreen = (props) => {
  return (
      <View>
        <Text>To be set or not to be set...</Text>
      </View>
  );
};

SettingsScreen.navigationOptions = {
  title: 'Settings',
};

export default SettingsScreen;